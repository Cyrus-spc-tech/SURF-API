

### Simple salary Prediction 

User → Streamlit UI
        ↓
  Streamlit sends data (HTTP POST)
        ↓
     FastAPI receives it
        ↓
   FastAPI calculates result
        ↓
   Streamlit displays result
